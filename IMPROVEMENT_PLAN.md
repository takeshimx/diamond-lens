# DiamondLens 再配線計画 — ハイレベル改善プラン

足りないのは部品ではない。Judge も評価基盤も監視も、すでに書かれている。
足りないのは、それらが本番につながり、ループとして回っていることだ。

> 根拠となる詳細な現状診断は [`TO_BE_AI_ENGINEERING.md`](./TO_BE_AI_ENGINEERING.md) にある。
> 本書はそこから導かれる「あるべき姿」と「そのための打ち手」だけを扱う。

## 現在地

| | |
|---|---|
| **1,303 行** | の Judge が、ランタイムから一度も呼ばれていない |
| **2 / 20** | CI で実行されているテストファイル |
| **約 3,200 行** | の到達不能なコードが本番ツリーに残る |
| **停止中** | デプロイ時の品質ゲート（コメントアウト） |

評価スクリプトはすでに使われていない旧経路を測り、本番の tool-calling ループは
一度も評価されていない。計器は精巧に作られているが、配線されていない。

---

## 軸 1: 非決定性を、感覚ではなく数値で統制する

**あるべき姿** — 本番と同じ経路を、同じ条件で、繰り返し測れている。品質が人の印象ではなく
分布として語られ、劣化はデプロイの前に止まる。

**そのために**

- **評価対象を本番経路に一致させる** — いま測っているのは使われていない旧 NLU。本番の tool-calling ループそのものを対象にする。
- **測る単位を「文章」から「ツール呼び出し」へ変える** — 応答文は揺れるが、どのツールをどの引数で呼んだかは決定的に判定できる。ここが最も費用対効果が高い。
- **単発の合否をやめ、3 回実行の一致率と分散で見る** — 非決定的なシステムに対する 1 回の pass/fail は情報を持たない。
- **評価を 3 層に分ける** — 毎 PR の無課金な契約検証、変更時のふるまい評価、本番トラフィックへの継続評価。層ごとにコストと確度が違う。
- **デプロイゲートを復活させる** — 測るだけでは止まらない。閾値を割ったらリリースが進まない状態にして初めて制御になる。

## 軸 2: 何が正で何が廃かを、読まずに分かる状態にする

**あるべき姿** — 移行は必ず終わり、フラグは残らない。コストと速度が数字で見え、
トレードオフの判断が個人の記憶に依存しない。

**そのために**

- **到達不能なコードを消す** — バックアップも旧エージェントも、残すべき履歴は Git が持っている。並存はそれだけで判断コストを全員に課す。
- **移行フラグに削除期限を持たせる** — 期限切れを CI が落とすようにすれば、フラグの墓場は構造的に発生しなくなる。
- **import 時の副作用をなくす** — 読み込み時にクライアントを生成しているせいで、最重要のテスト群が CI から外れている。ここは最短で最大の回復。
- **1 リクエストあたりのコストと遅延を PR で見せる** — 記録はすでに取れている。判断に使う場所へ運ぶ。

## 軸 3: エージェントが、自分の変更の正しさを自分で確かめられる

**あるべき姿** — 人は方針と検証基準を決め、実装と確認のループはエージェントが閉じる。
文脈は口頭ではなくリポジトリに置かれている。

**そのために**

- **リポジトリに地図を置く** — 生きている経路、触ってはいけない場所、変更後に通すべきコマンド。これがないと死んだファイルが編集される。
- **検証を 1 コマンドに畳む** — 手順が CI 設定とスクリプトに散っている限り、人もエージェントも「何をすれば正しいと言えるか」を知らない。
- **プロンプトをコードとして扱う** — 外部ファイルに出し、差分をレビューし、変更時は評価を必須にする。最も壊れやすく、最も検証されていない資産。

## 軸 4: 作るかどうかを、一つの指標で決められる

**あるべき姿** — 機能は増えるだけでなく減る。実装速度が上がったぶんの余力が、
「何を作るべきか」の判断に回っている。

**そのために**

- **北極星指標を決める** — 候補は「週あたり、満足して終わった分析セッション数」。決めた瞬間に、作る・消すの判断が自動的に決まる。
- **機能ごとの利用実態を測る** — いま測れているのは消費コストであって、ユーザが得た価値ではない。下位機能は明示的に廃止候補にする。
- **差別化点に絞って検証する** — 統計を引くだけなら既存サービスで足りる。固有の価値は複数データを統合した戦術的示唆にあり、そこだけを MVP として追う。
- **「何を聞けばいいか分からない」を解く** — tool-calling 型プロダクトの最大の離脱要因。機能追加より投資対効果が高い。

---

## 実行順序 — 先に配線を戻し、それから広げる

| Phase | やること | 期間 | これで何が変わるか |
|---|---|---|---|
| **00** | **出血を止める**<br>テストを CI に戻す／死コードを消す／地図と検証コマンドを置く | 1–2 日 | 最重要テストが回り始め、人もエージェントも正しい場所を編集できる |
| **01** | **評価を本番に合わせる**<br>ツール呼び出しベースの評価セットとハーネス／ゲート復活 | 3–5 日 | 本番のふるまいを初めて測れる。非決定性が数値になる |
| **02** | **ループを回す**<br>Judge を非同期で本番に結線／失敗の自動分類と起票／コスト可視化 | 1–2 週 | 死蔵していた資産が価値を出し、改善が思いつきでなくなる |
| **03** | **何を作るかを決める**<br>北極星指標の計測／機能の棚卸し／差別化点の検証 | 継続 | 実装者から構築者へ、役割が移る |

## いま、やらないこと

- **エージェントフレームワークへの再移行** — 素の SDK とツールループで足りている。基盤を整える前に載せ替えるのは順序が逆。
- **RAG の高度化** — まず「効いているか」を測る。測らずに高度化するのは典型的な浪費。
- **マルチエージェント化** — 品質を測れていない段階でエージェントを増やせば、非決定性が乗算的に増えるだけ。
- **サービス分割** — 現構成で捌けている。コストだけが増える。

---

このプロジェクトの技術的野心と実装量は際立っている。欠けているのは能力ではなく、
**書いたものを本番につなぎ、ループを閉じること**だけだ。
Phase 0 の 3 項目は 2 日で終わり、それだけでこのリポジトリは
「作られたもの」から「制御されているもの」へ変わる。
