name: 'mlb_analytics_dbt_project'
version: '1.0.0'
config-version: 2

profile: 'mlb_analytics_dbt_project'

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
